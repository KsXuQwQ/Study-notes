Java中 String 和 StringBuilder 的 区别核心是 ：可变性和线程安全（Spring线程可变且全，SpringBuilder 不可变 且线程不安全）

== 与 equals() 在Java中的区别： ==用于比较地址，equals比较内容（需重写）

Spring Boot中，启动类上的`@SpringBootApplication`注解包含哪三个子注解？：
 @Configuration, @EnableAutoConfiguration, @ComponentScan
 
@ComponentScan（扫描仪）
**职责**：告诉 Spring 去哪里找 Bean（组件）。
 